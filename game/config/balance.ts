export const GAME_BALANCE = {
  // Put game-specific tuning values here instead of scattering magic numbers.
  // Examples to add per game:
  // playerSpeed: 520,
  // enemySpawnMinDelay: 300,
  // difficultyScoreCap: 1000,
  // itemSpawnInterval: 100
};
